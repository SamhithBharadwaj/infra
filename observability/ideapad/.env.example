# Loki endpoint on druva (central stack)
LOKI_URL=http://druva:3100/loki/api/v1/push

# This node's hostname (used as label in logs)
NODE_HOSTNAME=ideapad

# Docker data path on ideapad (symlinked from /var/lib/docker)
DOCKER_LOG_PATH=/data/docker
